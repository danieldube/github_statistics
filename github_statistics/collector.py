"""
Data collection logic - assembling complete PR objects from the GitHub client.
"""


def collect_prs(_config, _options, _client):
    """Collect pull requests from configured repositories.

    Args:
        _config: Configuration object.
        _options: Runtime options (filters, date ranges).
        _client: GitHub client instance.

    Returns:
        List of PullRequest objects.

    This function will be implemented in Step 5.
    """
    raise NotImplementedError("collect_prs will be implemented in Step 5")
